Development guidelines:

- We want one development server running the background. If there isn't one, start it.
- If there already is a develpment server, don't add extras.
- If there are multiple dev servers running, kill them all and start one.
- Don't do production builds or attempt deployment unless requested by the user.
